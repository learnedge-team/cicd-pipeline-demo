version: '3.8'

services:
  app:
    build: ./sample-app
    ports:
      - "5000:5000"
    environment:
      - ENV=development
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3